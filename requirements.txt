fastapi
uvicorn[standard]
python-dotenv

# Database
# [asyncio] pulls in greenlet, which the async engine needs at runtime
sqlalchemy[asyncio]
aiosqlite
asyncpg
# Alembic runs synchronously, so it needs a sync driver alongside asyncpg
psycopg2-binary
alembic

# Auth / Security
passlib[bcrypt]
PyJWT
bcrypt==4.0.1

# Validation / schemas
pydantic
email-validator

# Rate limiting
slowapi
limits

# Email service
resend

# External APIs
openai

# Utilities
python-multipart
requests

# Excel import/export
pandas
openpyxl
